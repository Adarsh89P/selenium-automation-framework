# Environment: stage. Selected with -Denv=stage.
#
# TODO: point base.url at the staging deployment of ParaBank once one exists. The public demo
# is the only instance available today, so this file inherits it rather than pretending
# otherwise.
base.url=https://parabank.parasoft.com/parabank

# TODO: supply staging credentials through APP_USERNAME / APP_PASSWORD environment variables
# or GitHub Secrets. They are deliberately absent from this file.
