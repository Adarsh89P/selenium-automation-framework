# Environment: dev. Selected with -Denv=dev (the default).
#
# The application under test is Parasoft's public ParaBank demo. It is a deliberately
# public sandbox that resets periodically, so john/demo is demo data rather than a secret.
# Even so, both values are overridable by APP_USERNAME / APP_PASSWORD environment variables
# so the same suite can point at a private deployment without editing a file.

base.url=https://parabank.parasoft.com/parabank

app.username=john
app.password=demo
